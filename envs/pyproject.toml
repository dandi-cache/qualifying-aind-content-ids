[project]
name = "qualifying-aind-content-ids"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "remfile",
    "dandi",
    "spikeinterface",
    "datalad",
    "datalad-container",
]
