*.idea
__pycache__/
*.pyc
sourcedata/*

# Generated results live on the `derivatives` (and `min`) branches, not on `main`.
# Local runs without `--base-directory` write here; do not commit them to the code branch.
derivatives/
logs/
